# 5 formas de ejecutar nuastra API

    *Local probado
    *Ngrok probado
    *FastapiCloud (beta) probada !
    *GCRun function probado
    *GCRun docker 
        FastAPI Cloud doesn’t require any Docker configurations, it will build and run your app directly. Just ensure your pyproject.toml has the correct dependencies and Python version.
    *Hostgator: VPS, Host y Dominios.

